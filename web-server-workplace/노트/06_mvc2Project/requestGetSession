//세션객체 생성
		// request.getSession(); 매개변수가 없는 경우/true
		// -> 세션이 이미 존재하면 있는 세션을 가져오고
		// -> 세션이 없다면 새세션을 생성하여 반환!
		// 매개변수가 없는 경우 디폴트가 true.
		
		// request.getSession(false) 매개변수로 false
		//	기존에 세션이 존재하면 기존세션을 가져오고
		//	기존에 세션이 존재하지 않으면 null 리턴;		